# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# dependencies
node_modules/

# env
.env
.env.local

# macOS
.DS_Store

# Credenciales Google (Android) — nunca commitear
google-services.json
play-service-account.json
*firebase-adminsdk*.json

# Proyecto nativo generado por `expo run:android` (build local / emulador).
# NO commitear NI dejar que suba a EAS: con android/ presente en el upload,
# EAS saltea prebuild y el build cloud pasa a bare workflow.
/android/
