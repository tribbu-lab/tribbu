# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# dependencies
node_modules/

# env
.env
.env.local

# macOS
.DS_Store

# Credenciales Google (Android) — nunca commitear
google-services.json
play-service-account.json
*firebase-adminsdk*.json

# Proyectos nativos generados por `expo run:android` / `expo run:ios` (build
# local / emulador). NO commitear NI dejar que suban a EAS: con android/ o ios/
# presentes en el upload, EAS saltea prebuild y el build cloud pasa a bare workflow.
/android/
/ios/
