// Pantalla inicial — el gate del layout raíz redirige según la sesión.
import { Spinner } from "../components/Spinner";

export default function Index() {
  return <Spinner style={{ backgroundColor: "#0F172A" }} />;
}
